@model IEnumerable<JoiningTablesImplementation.Models.ViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StdName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.stdNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th></th>
        </tr>
       
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.StdName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.stdNum)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CourseName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.StartDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EndDate)
                        </td>
                        @*<td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.ViewKey }) |
                            @Html.ActionLink("Details", "Details", new { id = item.ViewKey }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.ViewKey })
                        </td>*@
                    </tr>
                }

         
    </table>
